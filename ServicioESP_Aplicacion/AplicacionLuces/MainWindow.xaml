<Window x:Class="AplicacionLuces.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AplicacionLuces"
        mc:Ignorable="d"
		Loaded="Window_Loaded"
        Title="Luces" Height="86.533" Width="150.307">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>
		<!--<DataGrid AutoGenerateColumns="False" Name="dgLuces">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" />
				<DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="2*" IsReadOnly="True" />
				<DataGridCheckBoxColumn Header="Estado" Binding="{Binding State}" IsReadOnly="True" />
			</DataGrid.Columns>
		</DataGrid>-->
		<WrapPanel>
			<Label Content="Estado" />
			<Label Name="lblName" />
		</WrapPanel>
		<Button Grid.Row="1" Content="Activar" Name="btnActivar" Click="Activar" />
	</Grid>
</Window>